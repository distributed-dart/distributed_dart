part of distributed_dart_test;

void scanner_test() {
  group('Scanner Tests -', () {
    group('Scanning files and check dependencies -', () {
      test('Test 1', () => expect(0, equals(0)));
      test('Test 2', () => expect(1, equals(1)));
    });
  });
}